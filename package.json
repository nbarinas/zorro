{
  "name": "study-validation-app",
  "version": "1.0.0",
  "description": "A web application to validate study completion based on user ID or phone number.",
  "main": "src/js/app.js",
  "scripts": {
    "start": "live-server src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Your Name",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "body-parser": "^1.19.0"
  },
  "devDependencies": {
    "live-server": "^1.2.1"
  }
}